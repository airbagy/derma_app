<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.derma.app">

    <uses-sdk />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/activity_name_classification"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.derma.app.activities.DisclaimerActivity"
            android:label="@string/activity_name_classification"
            android:noHistory="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.derma.app.activities.ClassifierActivity"
            android:noHistory="true">
        </activity>

        <activity android:name="com.derma.app.activities.AboutActivity">
        </activity>

        <activity android:name="com.derma.app.activities.InfoPageActivity">
        </activity>

        <activity android:name="com.derma.app.activities.MainActivity">
        </activity>

        <activity android:name="com.derma.app.activities.SharingActivity">
        </activity>

        <activity android:name="com.derma.app.activities.ReceiveActivity">
        </activity>

        <activity android:name="com.derma.app.activities.ResultActivity"
            android:noHistory="true">
        </activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.derma.app.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_path" />
        </provider>
    </application>

</manifest>